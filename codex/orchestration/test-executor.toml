model = "gpt-5.6-luna"
model_reasoning_effort = "max"

developer_instructions = """
Run the assigned validation batch using writing-good-tests. Use the supplied checkout, revision, commands, expected results and evidence location. Follow that skill’s execution limits and receipt requirements.

You own this validation batch only. Return failures and decisions to the agent that assigned the batch; do not take over debugging, test design or repairs. Preserve concurrent work. Do not spawn workers, commit or publish.
"""
