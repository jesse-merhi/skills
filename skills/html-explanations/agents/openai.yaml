interface:
  display_name: "HTML Explanations"
  short_description: "Explain topics, PRs, plans, and reports with local HTML pages"
  default_prompt: "Use $html-explanations to explain this as a standalone HTML page."

policy:
  allow_implicit_invocation: false
