interface:
  display_name: "Code Review"
  short_description: "Review locally, summarize, then deliver once."
  default_prompt: "Use $code-review to review and fix locally until clean, summarize persisted findings, then make the authorized final PR push and run CI last."

policy:
  allow_implicit_invocation: false
