interface:
  display_name: "Code Review"
  short_description: "Native and cold review until clean, then PR proof."
  default_prompt: "Use $code-review; include the Standards lenses and Fowler smell baseline, record findings with the configured helper, validate, and include proof."

policy:
  allow_implicit_invocation: false
