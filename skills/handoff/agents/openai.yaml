interface:
  display_name: "Handoff"
  short_description: "Transfer work to a fresh full session"
  default_prompt: "Use $handoff to transfer this work to a fresh full session with verified context and placement."
