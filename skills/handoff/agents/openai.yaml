interface:
  display_name: "Handoff"
  short_description: "Transfer work or delegate a mechanical phase"
  default_prompt: "Use $handoff to select a full-session transfer or one bounded mechanical worker with a verified return path."
