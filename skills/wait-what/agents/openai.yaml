interface:
  display_name: "Wait, What?"
  short_description: "Re-pitch an explanation that did not land"
  default_prompt: "Use $wait-what to re-pitch the previous explanation clearly."
policy:
  allow_implicit_invocation: false
