interface:
  display_name: "To Tickets"
  short_description: "Create blocker-aware tickets and PR groups."
  default_prompt: "Use $to-tickets to break this spec or plan into tracer-bullet Obsidian tickets, then derive cohesive standalone PRs or dependency-ordered gh-stack groups from the blocking graph."

policy:
  allow_implicit_invocation: false
