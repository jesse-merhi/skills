interface:
  display_name: "Design"
  short_description: "Design clear, deliberate product interfaces"
  default_prompt: "Use $design to improve this interface around its users, content, and existing design system."
