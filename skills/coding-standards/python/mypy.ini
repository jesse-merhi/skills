# Mypy half of the Python coding-standards preset.
# The options below are exactly the union of every `kind: "mypy"` options block
# in the python column of catalog.json; tests/test_catalog.py fails if they
# drift.

[mypy]
strict = True
disallow_any_explicit = True
warn_return_any = True
