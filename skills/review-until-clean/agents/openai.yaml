interface:
  display_name: "Review Until Clean"
  short_description: "Loop the native review to convergence"
  default_prompt: "Use $review-until-clean to run the tool-native review (codex review here) until it gets two clean passes."
