interface:
  display_name: "Wayfinder"
  short_description: "Map foggy multi-session work in Obsidian."
  default_prompt: "Use $wayfinder to chart this large effort as an Obsidian map with a destination, claimable decision tickets, blockers, and decisions so far."

policy:
  allow_implicit_invocation: false
