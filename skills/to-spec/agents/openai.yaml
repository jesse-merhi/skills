interface:
  display_name: "To Spec"
  short_description: "Synthesize a spec into Obsidian Specs."
  default_prompt: "Use $to-spec to turn this discussion into an Obsidian spec with implementation decisions, agreed testing seams, and open questions."

policy:
  allow_implicit_invocation: false
