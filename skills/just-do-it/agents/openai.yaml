interface:
  display_name: "Just Do It"
  short_description: "Resume a well-defined change through a ready PR."
  default_prompt: "Use $just-do-it to inspect the current implementation and PR state, resume at the earliest incomplete or stale checkpoint, and leave one green, reviewed PR ready for me."

policy:
  allow_implicit_invocation: false
