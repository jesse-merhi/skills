interface:
  display_name: "Feedback Hardening"
  short_description: "Harden evidence-backed reusable agent failures"
  default_prompt: "Use $feedback-hardening for an evidenced reusable agent failure: start its recommendation workflow or report the blocker, then implement only after approval."
