interface:
  display_name: "Feedback Hardening"
  short_description: "Move feedback into a separate task"
  default_prompt: "Use $feedback-hardening to launch a separate task with this session referenced and solve, then continue the main task independently."
