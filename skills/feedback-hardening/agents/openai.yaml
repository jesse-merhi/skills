interface:
  display_name: "Feedback Hardening"
  short_description: "Hand feedback to a fresh session to solve"
  default_prompt: "Use $feedback-hardening to immediately hand off to a fresh session, @mention this original session in its launch prompt, and say solve."
