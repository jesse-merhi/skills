interface:
  display_name: "Feedback Hardening"
  short_description: "Turn agent corrections into systemic fixes"
  default_prompt: "Use $feedback-hardening to turn this correction into a ranked systemic-fix recommendation, then wait for user approval before implementation."
