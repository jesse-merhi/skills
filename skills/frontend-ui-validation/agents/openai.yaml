interface:
  display_name: "Frontend UI Validation"
  short_description: "Audit rendered UI layout"
  default_prompt: "Use $frontend-ui-validation to check the changed UI with the project's existing browser or native tooling."
