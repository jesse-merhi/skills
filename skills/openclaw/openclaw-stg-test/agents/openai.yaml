interface:
  display_name: "OpenClaw Staging Test"
  short_description: "Expose disposable OpenClaw UI previews safely"
  default_prompt: "Use $openclaw-stg-test to share a temporary synthetic UI preview through the bundled launcher."
