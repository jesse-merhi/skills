interface:
  display_name: "Writing Good Tests"
  short_description: "Write useful tests for implemented behavior"
  default_prompt: "Use $writing-good-tests to prove distinct, credible failures in changed behavior and regressions, keep the coverage each one protects, and pass the required checks before pushing."
