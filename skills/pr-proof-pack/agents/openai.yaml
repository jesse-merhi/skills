interface:
  display_name: "PR Proof Pack"
  short_description: "Check and refresh practical PR proof."
  default_prompt: "Use $pr-proof-pack to check whether this PR's proof is current. Prefer a tightly edited process video, include matched before/after UI proof when comparable, upload stale github.com evidence with the repository command, and verify the rendered result headlessly or in a browser where required."
