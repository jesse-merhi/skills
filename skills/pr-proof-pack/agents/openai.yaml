interface:
  display_name: "PR Proof Pack"
  short_description: "Publish practical PR proof of working behavior."
  default_prompt: "Use $pr-proof-pack before creating or updating this PR, upload GitHub evidence with the existing gh token and browser fallback, then verify the rendered result headlessly or in a browser where required."
