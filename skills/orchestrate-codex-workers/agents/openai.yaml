interface:
  display_name: "Codex Worker Orchestration"
  short_description: "Delegate implementation with lightweight Sol guidance"
  default_prompt: "Use $orchestrate-codex-workers to delegate non-trivial implementation while Sol supplies important opinions, steering, and targeted review."
policy:
  allow_implicit_invocation: true
